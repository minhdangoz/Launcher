第一次从assets中读取到的CardInfo对应Manager没有数据
Passico软引用可能导致图片无法显示.(可能性非常小)
请求Card信息使用的CardInfo必须与CardsAdapter保持一致.同一个对象.
YDCardViewHolder对两种头部(一张图片||三张图片)监听最好使用匿名内部类,而非实现监听接口.
在刷新的时候需要调用:CardsListManager.loadCardList()

疑问:
BaseCardContentManager int mOurDefineChannelId;//所有子类都使用这个这个channel会不会出现问题----hui


解决问题:
1,刷新没有变化:
当下拉刷新时:
List<CardInfo> 会重新换获取(也就是与之前对象发生了变化,是不同的对象)----->
每一个CardInfo里面的CardContentManager对象在获取数据的时候,offset会从0开始.
关于offset的控制(初始化,递增,归零)应该独立于对象之外:
1,将offset设置为static达到类级别,初始化=0,offset=5,归零?无法解决
2,将offset存储在sharedPreference中.不同的频道,做为key值.---->频道key来源于CardInfo的cardSecondTypeId---->channelId
可以在sharedPreference中存储channelId以及对应的offset,但是如何销毁??
刷新时候修改偏移量offset?这个是全部修改偏移量,不可行.要使用单独channel修改偏移量offset.
独立于对象之外控制offset
用cardSecondTypeId做为key,还是channelId??---->CardSecondTypeId

2,
YiDianModel.images 的长度可以为0,但是你不可以为空----已解决

3,YDNewsAdapter点击没有响应
第一次初始化
当点击换一换的时候
数据加载(第一条新闻和剩下四条新闻是分开到不同的方法,还是一个方法)
---->一个方法:当点击换一换的时候,获取到数据直接把数据扔进来就行.当初始化的时候,

4,CardViewHolder中已定义整个Card的根目录:public ViewGroup cardView;所以在其子类中不应再重新定义View.例如:YDCardViewHolder


//
下拉刷新不灵,与List<CarInfo>获取不到有关.
sst

2016年04月19日09:55:07==========
问题:
1,adView获取到了两次而yokmob一次也没获取到;
2,今日头条内容么显示.
3,yokmob和adview目前使用的是在CardsAdapter中加载数据而不是Manager中
4,一点资讯没有更多XXX新闻&&对咨询内容没有图片过滤分类(容易出现无图情况)
5,给头条,yokmob,adview添加长时间获取不到数据的处理机制
6,定位不准,发送请求定位的位置
7,整理所有用到的第三方的key
8,打开方式有问题
推测:
1,接收的时候使用了相同的msg.what或者没有break;发送的时候使用将yokmob的Msg使用成了adview的msg----->没有break
2,CardsAdapter的List<CardInfo>,默认设置为null.等待更新
8,Bundle extras = new Bundle();
          extras.putString(OpenMode.OPEN_URL_KEY, mManager.getClickUrl());
          this.mCardInfo.open(mContext,extras);在CardsAdapter中有问题



